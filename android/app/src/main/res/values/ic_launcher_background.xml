<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Driver-app launcher icon background.
         Brand red. The "O" + arc foreground (white) sits on top, so
         the icon reads as "white wordmark on Rajlo red" — same
         palette riders + drivers see on every CTA across the app. -->
    <color name="ic_launcher_background">#F10100</color>
</resources>